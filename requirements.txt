# Runtime dependencies: what the web app needs to serve predictions.
numpy>=2.0,<2.3
pandas>=2.2,<2.4
# The CPU-only XGBoost build keeps Linux installs small (no CUDA libraries).
xgboost-cpu>=2.1,<2.2; sys_platform == "linux"
xgboost>=2.1,<2.2; sys_platform != "linux"
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0
python-multipart>=0.0.9
